{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-project bin/check-qol-arch-code.cjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-project bin/check-qol-arch-cross-platform.cjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-project bin/check-qol-arch-cicd.cjs"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-project bin/inject-qol-cli-context.cjs"
          }
        ]
      },
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/0.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/1.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/2.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/3.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/4.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/5.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/6.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/7.mjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const run=(stdin)=>cp.spawnSync(process.execPath,[file],{input:stdin});let payload=null;try{payload=JSON.parse(input)}catch{}if(payload?.tool_name!==\"apply_patch\"){const result=run(input);if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);}const calls=[];let current=null;for(const line of String(payload.tool_input?.command??payload.tool_input?.patch??\"\").split(\"\\n\")){const header=line.match(/^\\*\\*\\* (Add|Update) File: (.+)$/);if(header){current={kind:header[1],file:path.resolve(payload.cwd||process.cwd(),header[2].trim()),old:[],next:[]};calls.push(current);continue}if(line.startsWith(\"*** \")){if(!line.startsWith(\"*** Move to:\")&&!line.startsWith(\"*** End of File\"))current=null;continue}if(!current)continue;if(line.startsWith(\"@@\")){if(current.kind===\"Update\"&&(current.old.length||current.next.length)){current={kind:\"Update\",file:current.file,old:[],next:[]};calls.push(current)}continue}const body=line.slice(1);if(line[0]===\"+\")current.next.push(body);else if(line[0]===\"-\")current.old.push(body);else if(line[0]===\" \"){current.old.push(body);current.next.push(body)}}for(const call of calls){if(call.kind===\"Update\"&&!call.old.length&&!call.next.length)continue;const tool_input=call.kind===\"Add\"?{file_path:call.file,content:call.next.join(\"\\n\")+\"\\n\"}:{file_path:call.file,old_string:call.old.join(\"\\n\"),new_string:call.next.join(\"\\n\")};const result=run(JSON.stringify({...payload,tool_name:call.kind===\"Add\"?\"Write\":\"Edit\",tool_input}));if(result.stderr)process.stderr.write(result.stderr);if(String(result.stdout??\"\").trim()!==\"\"||result.status){if(result.stdout)process.stdout.write(result.stdout);process.exit(result.status??0)}}process.exit(0);' qol-project hooks/pinned-skills/8.mjs"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-project bin/qol-mission-reminder.cjs"
          }
        ]
      }
    ]
  }
}
