{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/commit-skill-context.cjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/commit-deny-coauthor.cjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/branch-deny-checkout-in-main-clone.cjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/branch-deny-agent-checkout.cjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/branch-deny-pid-branch-name.cjs"
          }
        ]
      },
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/deny-tool-matches.cjs"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/qol-cicd-context.cjs"
          },
          {
            "type": "command",
            "command": "node -e 'const fs=require(\"node:fs\"),cp=require(\"node:child_process\"),path=require(\"node:path\"),os=require(\"node:os\");const [plugin,rel]=process.argv.slice(1);const input=fs.readFileSync(0);let root=process.env.CLAUDE_PLUGIN_ROOT||process.env.CODEX_PLUGIN_ROOT||\"\";if(!root){const cache=path.join(process.env.CODEX_HOME||path.join(os.homedir(),\".codex\"),\"plugins\",\"cache\");const found=[];try{for(const market of fs.readdirSync(cache)){const dir=path.join(cache,market,plugin);if(!fs.existsSync(dir))continue;for(const version of fs.readdirSync(dir)){const candidate=path.join(dir,version);if(fs.existsSync(path.join(candidate,rel)))found.push([fs.statSync(candidate).mtimeMs,candidate]);}}}catch{}root=found.sort((a,b)=>a[0]-b[0]).pop()?.[1]||\"\";}if(!root)process.exit(0);const file=path.join(root,rel);if(!fs.existsSync(file))process.exit(0);const result=cp.spawnSync(process.execPath,[file],{input});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);process.exit(result.status??0);' qol-workflow bin/qol-monorepo-rules-context.cjs"
          }
        ]
      }
    ]
  }
}
